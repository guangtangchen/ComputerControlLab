该项目在“work4”基础上，将“work1”中的采样和输出用于改进原来的相应部分。
实现：
（1）两个模态
目前可以实现：
1）显示，
（1）左一显示：利用定时器0产生的定时中断（10ms）10ms*50=0.5秒产生的采样次数。成功
（2）左二显示：多路开关的路数						  成功
（3）右两位显示采样值，目前采用T*16+T来代替，成功
（4）调用控制器函数

下一步的工作：

（1）添加串口通讯模块		成功
（2）添加串口通讯调用部分	成功

（3）修改采样调理电路的比例系数，确定偏移量和相关的比例系数。

按照扬帆的调试经验，采用
    printf("s");  //默认的通过串口0发送信息，比较费时间，一般测试时用
    //  真正程序运行时需要将其屏蔽掉。
可以发现串口处的小灯闪亮，表示串口发数正确。

（4）经过台式机的实验，发现串口通信正常，通讯数据正确。

		


