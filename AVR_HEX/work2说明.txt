进行1毫秒动态显示。否则，没有余辉效应，就看不清所显示的内容。

只利用一个定时器time0，设置1毫秒的定时器。
（1）可以利用于显示
（2）再利用软件计数器，实现0.5秒的采样周期。

该项目实现：
（1）两个模态
目前可以实现：
1）显示，
（1）左一显示：利用定时器0产生的定时中断（10ms）10ms*50=0.5秒产生的采样次数。成功
（2）左二显示：黑						  成功
（3）右两位显示采样值，目前采用T*16+T来代替，成功
（4）调用控制器函数
（5）添加串口通讯及其调用     成功
（6）用台式机来检验，发现串口发送数据和接收数据正确。  成功
