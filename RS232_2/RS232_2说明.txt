模拟公司提供的软件，采用软件延迟1毫秒模块，进行动态显示。否则，没有余辉效应，就看不清所显示的内容。
另外利用一个定时器time0，可以设置10毫秒的定时器，再利用软件计数器，实现0.5秒的采样周期。

该项目实现：两个模态：工作模态（开关打向1）；非工作模态

其中工作模态实现：
1）显示，
（1）左一左二显示：接收上位机通过串口助手发来的一个16进制字节。成功

（2）利用定时器0产生的定时中断（10ms）10ms*50=0.5秒产生的采样次数。
右两位显示：T*16+(T+1)；并串口发送这两位数。成功
